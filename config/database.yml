default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: <%= ENV.fetch("MYSQL_USERNAME", "root") %>
  password: <%= ENV.fetch("MYSQL_PASSWORD", "password") %>
  host: <%= ENV.fetch("MYSQL_HOST", "db") %>

development:
  <<: *default
  database: WebMedia_development

test:
  <<: *default
  database: WebMedia_test

production:
  <<: *default
  database: WebMedia_production
  username: WebMedia
  password: <%= ENV['MYAPP_DATABASE_PASSWORD'] %>